{{!-- This is the shell of the webpage, only the information inside it changes --}}
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Tech-Blog</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
    </head>

    <body>
        <div class="wrapper">
            <header>
                <h1>
                    <a href="/">Tech-Blog</a>
                </h1>
                <nav>
                    {{#if loggedIn}}
                    <a href="/dashboard">dashboard</a>
                    <button id="logout" class="btn-no-style" id="logout-button">logout</button>
                    {{else}}
                    <a href="/login">dashboard</a>
                    <a href="/login">login</a>
                    {{/if}}
                </nav>
            </header>
            <main>
                {{{ body }}}
            </main>
            <footer>
                Talk technology!
            </footer>
        </div>
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>

<script